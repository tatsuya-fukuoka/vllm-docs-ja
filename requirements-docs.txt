# vLLM ドキュメント日本語版のビルドに必要なパッケージ
# 上流 (vllm-project/vllm) の requirements/docs.txt から、
# vLLM 本体の import を必要とするもの (mkdocstrings / api-autonav / gen-files) を除いた構成。
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-awesome-nav==3.1.2
mkdocs-glightbox==0.5.1
mkdocs-git-revision-date-localized-plugin==1.4.7
# Pygments 2.20 以降と組み合わせるには 11.0 以上が必要
# (10.x はコードブロックのタイトル未指定時に html.escape(None) で落ちる)
pymdown-extensions==11.0.1
Pygments==2.20.0
# hooks/url_schemes.py が利用（可変長 lookbehind のため標準 re ではなく regex）
regex==2025.7.34
